<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 

<mapper namespace="chart">

<insert id="insertmychart" parameterType="com.final2.petopia.model.ChartVO">
  insert into  chart(chart_UID,fk_pet_UID,fk_idx,chart_type,biz_name,bookingdate
                   ,reservation_DATE,doc_name,cautions,chart_contents,
                   payment_pay,payment_point,addpay,totalpay) 
  values(chart_UID.nextval(),#{},#{},#{},#{},#{DOCNAME},#{CAUTION},#{MEMO},default,default,default,default,)

</insert>

<select id="selectpetuid" >
select pet_UID 
from pet_info A join Member B
on A.fk_idx= B.idx;
</select>
</mapper>