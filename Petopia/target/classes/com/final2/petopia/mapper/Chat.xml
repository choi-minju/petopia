<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 

<mapper namespace="chat">

<!-- idx에 따라 알람테이블에 code 삽입 -->
 <insert id="addcode" parameterType="HashMap">
	insert into notification (not_UID, fk_idx, not_type, not_message, not_date, not_readcheck)
	values(seq_notification_UID.nextval, #{idx}, 5, #{code}, sysdate, default)
</insert>

</mapper>