<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 

<mapper namespace="reservation">
	<select id="selectScheduleListByIdx_biz" parameterType="String" resultType="com.final2.petopia.model.ScheduleVO">
		select fk_idx_biz, to_char(schedule_date, 'yyyy-mm-dd hh24:mi') as schedule_date, schedule_status, to_char((schedule_date+30/(24*60)), 'yyyy-mm-dd hh24:mi') as endtime
		from schedule
		where fk_idx_biz=#{idx_biz}
		order by schedule_date
	</select>
</mapper>